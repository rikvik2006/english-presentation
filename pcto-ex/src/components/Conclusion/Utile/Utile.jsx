import styles from "./Utile.module.scss"

const Utile = () => {
    return (
        <div className={styles.titleContainer}>
            <div className={styles.title}>utile?</div>
        </div>
    );
};

export default Utile;